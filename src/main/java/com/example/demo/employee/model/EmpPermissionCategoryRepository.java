package com.example.demo.employee.model;

import org.springframework.data.jpa.repository.JpaRepository;

public interface EmpPermissionCategoryRepository extends JpaRepository<EmpPermissionCategory, Long> {
}
